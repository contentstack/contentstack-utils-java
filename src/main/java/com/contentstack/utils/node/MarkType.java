package com.contentstack.utils.node;


// The code snippet is defining an enumeration called `MarkType`. An enumeration is a special type in
// Java that represents a fixed set of constants. In this case, the `MarkType` enumeration represents
// different types of text formatting marks.
public enum MarkType {
    BOLD, ITALIC, UNDERLINE, STRIKETHROUGH, INLINECODE, SUBSCRIPT, SUPERSCRIPT, BREAK,
}
